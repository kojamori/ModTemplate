name: Publish NuGet Package

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: windows-latest

    permissions:
      contents: read
      id-token: write # Required for NuGet Trusted Publishing (OIDC)

    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Restore
        run: dotnet restore

      - name: Pack Template
        shell: pwsh
        run: |
          $out = Join-Path $PWD "bin\Release"
          dotnet pack ".\kojamori.SFS.Templates.CodeMod.csproj" -c Release -o $out

      - name: NuGet login (OIDC -> temp API key)
        uses: NuGet/login@v1
        with:
          user: kojamori # Replace with your exact NuGet.org username

      - name: Push to NuGet.org
        shell: pwsh
        run: |
          dotnet nuget push "bin\Release\*.nupkg" --source "https://api.nuget.org/v3/index.json" --skip-duplicate
